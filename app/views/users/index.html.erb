<p id="notice"><%= notice %></p>

<h1>Amigo Secreto</h1>
<div class="users_buttons">
  <%= button_to 'Novo Usuário', new_user_path, class: 'btn btn-success' %>
  <%= button_to 'Sortear', raffle_users_path, class: 'btn btn-primary' %>
</end>
</div>
<table class="table table-inverse">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Amigo Secreto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= user.friend.try(:name) %></td>
        <td><%= link_to 'Editar', edit_user_path(user) %></td>
        <td><%= link_to 'Deletar', user, method: :delete, data: { confirm: 'Tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<br>
